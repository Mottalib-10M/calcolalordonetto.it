---
import CalcolatoreLayout from '../../layouts/CalcolatoreLayout.astro';
import PianoAccumulo from '../../components/calcolatori/PianoAccumulo';
import { SITE } from '../../config';
import { canonicalURL } from '../../lib/seo';

const lang = 'en' as const;
const title = "PAC Savings Plan Calculator: Monthly Investment Returns";
const description = "Project how monthly investments grow over time with a PAC (Piano di Accumulo). See total invested, projected returns, capital gains tax, and final portfolio.";
const canonical = canonicalURL('/en/savings-plan-calculator/');

const breadcrumbs = [
  { name: 'Home', url: `${SITE.url}/en/` },
  { name: 'Savings Plan Calculator', url: canonical },
];
---

<CalcolatoreLayout
  title={title}
  description={description}
  calculatorName="Savings Plan Calculator"
  breadcrumbs={breadcrumbs}
  canonical={canonical}
  lang={lang}
>
  <PianoAccumulo client:load lang={lang} />
</CalcolatoreLayout>
